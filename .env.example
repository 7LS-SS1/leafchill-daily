DATABASE_URL=postgresql://postgres:postgres@localhost:5432/leaf_chill_daily?schema=public
APP_ENV=production
NODE_ENV=production
PORT=3000
# Set to false only when testing production over plain HTTP.
COOKIE_SECURE=true
